{
  "name": "sociavault-mcp-worker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Hosted (remote) SociaVault MCP server on Cloudflare Workers. Streamable HTTP transport, stateless, free-tier friendly.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.20.0"
  }
}
